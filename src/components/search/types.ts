export interface SearchFilters {
  minSurface: number;
  maxSurface: number;
  minPrice: number;
  maxPrice: number;
  city: string;
  district: string;
  propertyType: string;
  fiscalStatus: string;
}